import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { PrivateRoutes } from './screen/private'

const Routes = () => {
    return (
        <PrivateRoutes />
    )
}

export default Routes

const styles = StyleSheet.create({})